<ips:template parameters="$url, $newWindow=TRUE, $title=NULL, $wordbreak=TRUE, $nofollow=FALSE" />
{{if $wordbreak}}<div class='ipsType_break ipsContained'>{{endif}}<a href='{$url}'{{if $newWindow === TRUE}} target='_blank' {{if $nofollow === FALSE}}rel="noopener"{{endif}}{{endif}}{{if $nofollow === TRUE}} rel="nofollow{{if $newWindow === TRUE}} noopener{{endif}}"{{endif}}>{{if $title}}{$title}{{else}}{$url}{{endif}}</a>{{if $wordbreak}}</div>{{endif}}